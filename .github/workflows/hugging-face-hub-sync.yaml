name: Sync to Hugging Face hub
on:
  push:
    branches: [ main ]

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "github-actions"
          
          # Configure Hugging Face repository using HTTPS with token
          git remote add hub https://$HF_TOKEN@huggingface.co/kernels-community/mrope_get_position_ids.git
          
          # Push to Hugging Face
          git push hub main:main